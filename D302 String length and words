#include <bits/stdc++.h>
using namespace std;
int main() {
  int b=0, c=0;
  string a;
  getline(cin,a);
  int l= a.length();
    for(int i=0; i<=l; i++){
      if(a[i]==' '){
        c++;
      }
    }
  cout << l << endl << (c+1);
}
