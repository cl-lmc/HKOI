#include <bits/stdc++.h>
using namespace std;
int main() {
  int c;
  string a,b;
  getline(cin,a);
  c = a.length();
  b.resize(c);
  for(int i=c; i >= 0; i--){
    b[i-1] = a[c-i];
  }
  cout << b <<endl;
  if(a==b){
    cout << "Yes";
  }
  else{
    cout << "No";
  }
}
