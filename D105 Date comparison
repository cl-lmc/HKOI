#include <bits/stdc++.h>
using namespace std;
int main() {
  int a,b,c,d,e,f;
  cin >> a >> b >> c >> d >> e >> f;
  if((a*100000 + b*100 + c) > (d*100000 + e*100 + f)){
    cout << "After";
  }
  else if((a*100000 + b*100 + c) < (d*100000 + e*100 + f)){
    cout << "Before";
  }
  else{
    cout << "Same";
  }
}
